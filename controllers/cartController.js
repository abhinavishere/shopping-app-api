/**
 * Get the items in current user's cart
 * @private getCurrentUserCart
 * @param {Request} req
 * @param {Response} res
 */
function getCurrentUserCart(req, res) {}

/**
 * Add Item to the cart
 * @private addItemToCart
 * @param {Request} req
 * @param {Response} res
 */
function addItemToCart(req, res) {}

/**
 * UpdateItemsInCart
 * @private updateItemInCart
 * @param {Request} req
 * @param {Response} res
 */
function updateItemInCart(req, res) {}

/**
 * Remove an item from cart
 * @private removeItemFromCart
 * @param {Request} req
 * @param {Response} res
 */
function removeItemFromCart(req, res) {}

module.exports = {
  getCurrentUserCart,
  addItemToCart,
  updateItemInCart,
  removeItemFromCart,
};
