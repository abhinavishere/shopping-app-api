/**
 * Get current logged in user info
 * @param {Request} req
 * @param {Response} res
 */
function getCurrentUserInfo(req, res) {}

/**
 * Edit current logged in user info
 * @param {Request} req
 * @param {Response} res
 */
function updateCurrentUserInfo(req, res) {}

/**
 * Get all users info
 * @private getAllUsers
 * @param {Request} req
 * @param {Response} res
 */
function getAllUsers(req, res) {}

/**
 * Update user info using user id
 * @private updateUserInfo
 * @param {Request} req
 * @param {Response} res
 */
function updateUserInfo(req, res) {}

/**
 * Delete a user
 * @private deleteUser
 * @param {Request} req
 * @param {Response} res
 */
function deleteUser(req, res) {}

module.exports = {
  getCurrentUserInfo,
  updateCurrentUserInfo,
  getAllUsers,
  updateUserInfo,
  deleteUser,
};
